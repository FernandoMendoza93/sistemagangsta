# ============================================
# Railway Configuration
# The Gangsta Barber Shop
# ============================================

[build]
# Usar el Dockerfile de la raíz
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
